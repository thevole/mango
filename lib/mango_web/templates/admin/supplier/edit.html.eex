<h2>Edit Supplier</h2>

<%= render "form.html", Map.put(assigns, :action, admin_supplier_path(@conn, :update, @supplier)) %>

<span><%= link "Delete", to: admin_supplier_path(@conn, :delete, @supplier), method: [confirm: "Are you sure?"], class: "btn btn-danger" %></span>
